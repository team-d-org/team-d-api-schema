id:
  properties:
    id:
      type: integer
amount:
  properties:
    amount:
      type: integer
balance:
  properties:
    balance:
      type: integer

# enum
kind:
  properties:
    kind:
      type: string
# enum
type:
  properties:
    type:
      type: string
vendor_name:
  properties:
    vendor_name:
      type: string
# unixtime
created_at:
  properties:
    created_at:
      type: integer

# for array
all:
  type: object
  properties:
    id:
      type: integer
    amount:
      type: integer
    balance:
      type: integer
    # enum
    kind:
      type: string
    # enum
    type:
      type: string
    vendor_name:
      type: string
    # unixtime
    created_at:
      type: integer
